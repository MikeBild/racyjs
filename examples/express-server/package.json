{
  "name": "express-server",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "racy dev",
    "build": "racy build",
    "serve": "racy serve",
    "start": "NODE_ENV=production racy build && racy serve"
  },
  "dependencies": {
    "compression": "^1.7.3",
    "cors": "^2.8.4",
    "morgan": "^1.9.1",
    "racy": "file:../.."
  },
  "devDependencies": {
    "@babel/core": "^7.1.2"
  }
}
