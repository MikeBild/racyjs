{
  "private": true,
  "name": "react-graphql-link",
  "version": "1.0.0",
  "scripts": {
    "dev": "racy dev",
    "build": "racy build",
    "serve": "racy serve",
    "export": "racy export",
    "start": "NODE_ENV=production racy build && racy serve",
    "static": "NODE_ENV=production npm run export && racy static"
  },
  "dependencies": {
    "apollo-link": "^1.0.0",
    "apollo-link-state": "^0.4.2",
    "racy": "file:../.."
  },
  "devDependencies": {
    "@babel/core": "^7.1.2"
  }
}
