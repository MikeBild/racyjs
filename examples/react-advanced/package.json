{
  "private": true,
  "name": "racy-react-advanced",
  "version": "1.0.0",
  "scripts": {
    "dev": "racy dev",
    "build": "racy build",
    "serve": "racy serve",
    "export": "racy export",
    "start": "NODE_ENV=production racy build && racy serve",
    "static": "NODE_ENV=production npm run export && racy static"
  },
  "dependencies": {
    "racy": "file:../.."
  },
  "devDependencies": {
    "@babel/core": "^7.1.2"
  }
}
